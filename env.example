# CAPM Analysis - Environment Variables Template
# 
# Copy this file to .env and fill in your actual credentials
# DO NOT commit .env to version control (it's in .gitignore)
#
# Usage:
#   cp env.example .env
#   Then edit .env with your actual values

# ============================================
# WRDS Credentials (Optional)
# ============================================
# Required only if using WRDS data source
# Get WRDS access: https://wrds-www.wharton.upenn.edu/
WRDS_USERNAME=your_wrds_username
WRDS_PASSWORD=your_wrds_password

# Alternative: Use .pgpass file in home directory
# Format: hostname:port:database:username:password
# See: https://www.postgresql.org/docs/current/libpq-pgpass.html

# ============================================
# FRED API Key (Optional)
# ============================================
# Required only if using FRED data source
# Get free API key: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=your_fred_api_key_here

# ============================================
# Notes
# ============================================
# - All credentials are optional - the code will work without them
# - If credentials are not set, the code will use fallback data sources
# - Never commit .env file to version control
# - Share env.example (this file) but not .env

